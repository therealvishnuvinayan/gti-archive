generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["relationJoins"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum UserRole {
  SUPER_ADMIN
  ADMIN
  COLLABORATOR
  USER
}

enum CollaboratorType {
  GTI_INTERNAL_CLIENT
  GTI_SISTER_COMPANY_INTERNAL_CLIENT
  EXTERNAL_FREELANCER
  EXTERNAL_AGENCY
  EXTERNAL_VENDOR
  CLIENT_OF_GTI
}

enum ProjectCollaboratorParticipantType {
  GTI_INTERNAL_CLIENT
  GTI_SISTER_COMPANY_INTERNAL_CLIENT
  EXTERNAL_FREELANCER
  EXTERNAL_AGENCY
  EXTERNAL_VENDOR
  CLIENT_OF_GTI
}

enum StageStatus {
  PENDING
  ONGOING
  ON_HOLD
  COMPLETED
}

enum ProjectExecutionType {
  INTERNAL
  EXTERNAL
}

enum ProjectPriority {
  LOW
  MEDIUM
  HIGH
  URGENT
}

enum ProjectWorkflowStageKey {
  PROJECT_INQUIRY
  PROJECT_RESEARCH_AND_PLANNING
  CONCEPT_CREATION
  PROJECT_DEVELOPMENT
  FINAL_LAYOUT
  PRODUCTION_AND_HANDOVER
  IMPLEMENTATION_AND_SUPERVISION
}

enum ProjectWorkflowStageStatus {
  LOCKED
  AVAILABLE
  COMPLETED
}

enum ProjectInquiryClientOrigin {
  INTERNAL
  EXTERNAL
}

enum ProjectInquiryPartyRole {
  CLIENT
  FINAL_BENEFICIARY
}

enum ProjectInquiryPartySource {
  USER
  MANUAL_CONTACT
}

enum ProjectInquiryTargetMarketKind {
  COUNTRY
  REGION
  GLOBAL
}

enum ProjectInquiryPriority {
  LOW
  MEDIUM
  HIGH
}

enum ProjectInquiryAttachmentField {
  INITIAL_BRIEF
  BUSINESS_OBJECTIVES
  LEGAL_NOTES
}

enum ProjectFileChecklistField {
  OUTPUT_NAME
  TECHNICAL_DRAWING
  HEALTH_WARNING
  TAR
  NICOTINE
  // Retained for historical rows and approval snapshots; new checklists use TAR and NICOTINE.
  TAR_NICOTINE
  COMPULSORY_TEXT
  MARKETING_COPY
  RELATED_GRAPHICS
  PRINTING_TECHNOLOGY
  FINISHES
  BARCODE
  TRACK_TRACE
  THREEDS
  TAX_STAMP
  QR_CODE
  INVOICE
}

enum ProjectFileChecklistItemStatus {
  PENDING
  REQUESTED
  FILLED
}

enum ProjectFileChecklistRequestChannel {
  IN_APP
  EMAIL
}

enum ProjectFileChecklistRequestStatus {
  PENDING
  SENT
  FAILED
  CANCELLED
}

enum ProjectFileChecklistRequestWorkflowStatus {
  REQUESTED
  ACCEPTED
  COMPLETED
  DECLINED
  CANCELLED
}

enum ProjectFileChecklistResponseSource {
  AUTHENTICATED_USER
  EXTERNAL_EMAIL
}

enum ProjectProductionUnitStatus {
  PREPARATION
  APPROVAL_PENDING
  REJECTED
  HANDOVER_READY
  HANDED_OVER
}

enum ProductionSupervisionStatus {
  NOT_STARTED
  IN_REVIEW
  REVISIONS_NEEDED
  SIGNED_OFF
}

enum ProductionSampleRoundType {
  PRE_PRODUCTION_SAMPLE
  PRODUCTION_SAMPLE
  FINAL_MASS_PRODUCTION_SIGN_OFF
  CUSTOM
}

enum ProductionSampleRoundStatus {
  PENDING
  UNDER_REVIEW
  COMPLETED
}

enum ProductionSampleDecision {
  PASS
  FAIL
  CONDITIONAL
}

enum PhysicalSampleDecision {
  ACCEPTED
  REJECTED
}

enum ProductionSampleCriterion {
  MATERIAL_QUALITY
  GRAPHIC_REPRODUCTION
  SIZE
  CONSTRUCTION
  GRAPHIC_ELEMENTS
  FUNCTIONALITY
  FINISHES
}

enum ProductionApprovalRecipientType {
  EXISTING_COLLABORATOR
  EXTERNAL_EMAIL
}

enum ProductionApprovalStepStatus {
  WAITING
  ACTIVE
  APPROVED
  REJECTED
}

enum ProductionDispatchStatus {
  NOT_SENT
  PENDING
  SENT
  FAILED
}

enum ProductionHandoverRoute {
  PURCHASE_DEPARTMENT
  DIRECT_VENDOR
}

enum ProductionHandoverDeliveryStatus {
  PENDING
  SENT
  FAILED
}

enum ProjectAttachmentUploadSource {
  AUTHENTICATED_USER
  EXTERNAL_CHECKLIST_REQUEST
}

enum CalendarEventType {
  PROJECTS
  EVENTS
  REMINDERS
  PAYMENTS
}

enum CalendarEventTone {
  GREEN
  PURPLE
  BLUE
  AMBER
}

enum AttachmentAssetType {
  REVISION_ORIGINAL
  STAGE_SUBMISSION
  REVISION_PREVIEW
  REVISION_THUMBNAIL
  COMMENT_ATTACHMENT
  STAGE_INVOICE
  FINAL_ARCHIVE
  GENERAL_PROJECT_ASSET
  PROJECT_RESEARCH_FILE
  FILE_CHECKLIST_ATTACHMENT
  SAMPLE_ROUND_EVIDENCE
}

enum ProjectResearchFolderSystemKey {
  BRIEF
  MARKET_COMPETITION
  TECH
  VENDORS
  FINANCE
  LEGAL
  PITCH
}

enum AttachmentStatus {
  UPLOADING
  READY
  FAILED
  DELETED
}

enum SubmissionReviewStatus {
  PENDING_REVIEW
  APPROVED
  REJECTED
}

enum ProjectRevisionStatus {
  PENDING_REVIEW
  APPROVED
  REJECTED
}

enum ActivityLogAction {
  ASSET_UPLOADED
  COMMENT_ATTACHMENT_UPLOADED
  REVISION_CREATED
  ARCHIVE_SNAPSHOT_SAVED
  FINAL_ARCHIVED
}

enum ArchiveRecordStatus {
  SAVED
  COMPLETED
  ARCHIVED
}

enum ArchiveAccessLevel {
  NONE
  FULL
  PARTIAL
}

enum ProjectCompletionStepStatus {
  NOT_STARTED
  NOT_REQUIRED
  PENDING
  COMPLETED
}

enum ProjectCompletionDocumentType {
  AUTHORITY_APPROVAL_PROOF
  COPYRIGHT_TRANSFER
  INVOICE
}

enum NotificationType {
  PROJECT_ASSIGNED
  PROJECT_CREATED
  PROJECT_UPDATED
  COLLABORATOR_ADDED
  COLLABORATOR_REMOVED
  BRIEF_ACCEPTED
  REVISION_SUBMITTED
  REVISION_APPROVED
  REVISION_REJECTED
  SUBMISSION_PENDING_REVIEW
  SUBMISSION_COMPLETED
  SUBMISSION_REVISION_REQUESTED
  STAGE_COMPLETED
  NEXT_STAGE_ACTIVATED
  COMMENT_ADDED
  MENTION
  FILE_UPLOADED
  PROJECT_ARCHIVED
  APPROVAL_REQUIRED
  APPROVAL_PROOF_UPLOADED
  COPYRIGHT_TRANSFER_REQUIRED
  COPYRIGHT_DOCUMENT_UPLOADED
  INVOICE_REQUESTED
  INVOICE_UPLOADED
  CHECKLIST_INFORMATION_REQUESTED
  CHECKLIST_INFORMATION_COMPLETED
  CHECKLIST_INFORMATION_DECLINED
  PRODUCTION_APPROVAL_REQUESTED
  PRODUCTION_APPROVAL_APPROVED
  PRODUCTION_APPROVAL_REJECTED
  PRODUCTION_HANDOVER_COMPLETED
  STAGE_SEVEN_OVERDUE
  PRODUCTION_SAMPLE_FEEDBACK
}

enum NotificationEntityType {
  PROJECT
  STAGE
  REVISION
  COMMENT
  ATTACHMENT
  ARCHIVE
  COMPLETION_WORKFLOW
  COMPLETION_DOCUMENT
  CHECKLIST_REQUEST
  PRODUCTION_UNIT
  PRODUCTION_APPROVAL
  PRODUCTION_SUPERVISION
  SAMPLE_ROUND
}

enum FluxAiConversationStatus {
  ACTIVE
  ARCHIVED
}

enum FluxAiMessageRole {
  USER
  ASSISTANT
  SYSTEM_EVENT
}

model User {
  id                                  String                               @id @default(cuid())
  email                               String                               @unique
  name                                String?
  avatarUrl                           String?
  department                          String?
  phoneNumber                         String?
  jobTitle                            String?
  bio                                 String?
  passwordHash                        String
  passwordChangedAt                   DateTime?
  role                                UserRole                             @default(COLLABORATOR)
  collaboratorType                    CollaboratorType                     @default(GTI_INTERNAL_CLIENT)
  inviteToken                         String?                              @unique
  inviteExpiresAt                     DateTime?
  inviteAcceptedAt                    DateTime?
  sessions                            Session[]
  passwordResetTokens                 PasswordResetToken[]
  projects                            Project[]                            @relation("ProjectCreator")
  ownedProjects                       Project[]                            @relation("ProjectOwner")
  projectCoOwnerships                 ProjectCoOwner[]                     @relation("ProjectCoOwnerUser")
  addedProjectCoOwnerships            ProjectCoOwner[]                     @relation("ProjectCoOwnerAdder")
  revisions                           ProjectRevision[]
  reviewedRevisions                   ProjectRevision[]                    @relation("ProjectRevisionReviewer")
  comments                            ProjectComment[]
  deletedProjectComments              ProjectComment[]                     @relation("ProjectCommentDeletedBy")
  commentMentions                     ProjectCommentMention[]
  comparisonComments                  ComparisonComment[]
  attachments                         ProjectAttachment[]
  projectArchives                     ProjectArchive[]                     @relation("ProjectArchiveArchivedBy")
  archivedProjectFiles                ArchivedProjectFile[]                @relation("ArchivedProjectFileArchivedBy")
  createdArchiveArtworkMetadata       ArchiveArtworkMetadata[]             @relation("ArchiveArtworkMetadataCreatedBy")
  approvedArchiveArtworkMetadata      ArchiveArtworkMetadata[]             @relation("ArchiveArtworkMetadataApprovedBy")
  archivedArtworkMetadata             ArchiveArtworkMetadata[]             @relation("ArchiveArtworkMetadataArchivedBy")
  approvalContactWorkflows            ProjectCompletionWorkflow[]          @relation("ProjectCompletionApprovalContact")
  copyrightContactWorkflows           ProjectCompletionWorkflow[]          @relation("ProjectCompletionCopyrightContact")
  invoiceContactWorkflows             ProjectCompletionWorkflow[]          @relation("ProjectCompletionInvoiceContact")
  uploadedCompletionDocuments         ProjectCompletionDocument[]          @relation("ProjectCompletionDocumentUploadedBy")
  manualArchiveFiles                  ManualArchiveFile[]                  @relation("ManualArchiveFileUploadedBy")
  archiveAccess                       UserArchiveAccess?                   @relation("UserArchiveAccessUser")
  archiveAssetAccesses                UserArchiveAssetAccess[]             @relation("UserArchiveAssetAccessUser")
  archiveCategoryAccesses             ArchiveCategoryAccess[]              @relation("ArchiveCategoryAccessUser")
  grantedArchiveAccesses              UserArchiveAccess[]                  @relation("UserArchiveAccessGrantedBy")
  grantedArchiveAssetAccesses         UserArchiveAssetAccess[]             @relation("UserArchiveAssetAccessGrantedBy")
  createdArchiveCategoryAccesses      ArchiveCategoryAccess[]              @relation("ArchiveCategoryAccessCreatedBy")
  fluxAiConversations                 FluxAiConversation[]
  fluxAiSearchHistory                 FluxAiSearchHistory[]
  manualLibraryAssets                 ManualLibraryAsset[]                 @relation("ManualLibraryAssetUploadedBy")
  notifications                       Notification[]
  fileFavorites                       FileFavorite[]
  manualLibraryAssetFavorites         ManualLibraryAssetFavorite[]
  activityLogs                        ProjectActivityLog[]
  createdCollaboratorVisibilityPauses ProjectCollaboratorVisibilityPause[] @relation("ProjectCollaboratorVisibilityPauseCreator")
  projectExecutorAssignments          ProjectExecutor[]                    @relation("ProjectExecutorUser")
  addedProjectExecutorAssignments     ProjectExecutor[]                    @relation("ProjectExecutorAddedBy")
  startedProjectStages                ProjectStage[]                       @relation("ProjectStageStarter")
  reviewedStageSubmissions            ProjectAttachment[]                  @relation("SubmissionReviewer")
  stageInvoiceRequestsMade            StageInvoiceRequest[]                @relation("StageInvoiceRequestRequester")
  stageInvoiceRequestsReceived        StageInvoiceRequest[]                @relation("StageInvoiceRequestRecipient")
  calendarEvents                      CalendarEvent[]
  projectCollaborations               ProjectCollaborator[]                @relation("ProjectCollaboratorUser")
  addedProjectCollaborations          ProjectCollaborator[]                @relation("ProjectCollaboratorAdder")
  calendarCollaborations              CalendarCollaborator[]               @relation("CalendarCollaboratorUser")
  addedCalendarCollaborations         CalendarCollaborator[]               @relation("CalendarCollaboratorAdder")
  createdContactDirectoryEntries      ContactDirectoryEntry[]              @relation("ContactDirectoryEntryCreator")
  projectInquiryPartySelections       ProjectInquiryParty[]                @relation("ProjectInquiryPartyUser")
  researchWorkspaces                  ProjectResearchWorkspace[]           @relation("ProjectResearchWorkspaceOwner")
  createdResearchFolders              ProjectResearchFolder[]              @relation("ProjectResearchFolderCreator")
  addedResearchFolderFiles            ProjectResearchFolderFile[]          @relation("ProjectResearchFolderFileAdder")
  createdConceptFolders               ProjectConceptFolder[]               @relation("ProjectConceptFolderCreator")
  approvedConceptFolders              ProjectConceptFolder[]               @relation("ProjectConceptFolderApprover")
  createdStageFileHandoffs            ProjectStageFileHandoff[]            @relation("ProjectStageFileHandoffCreator")
  updatedFileChecklistItems           ProjectFileChecklistItem[]           @relation("ProjectFileChecklistItemUpdater")
  fileChecklistRequestsMade           ProjectFileChecklistRequest[]        @relation("ProjectFileChecklistRequestRequester")
  fileChecklistRequestsReceived       ProjectFileChecklistRequest[]        @relation("ProjectFileChecklistRequestRecipient")
  fileChecklistRequestsResponded      ProjectFileChecklistRequest[]        @relation("ProjectFileChecklistRequestResponder")
  createdProductionUnits              ProjectProductionUnit[]              @relation("ProjectProductionUnitCreator")
  addedProductionUnitFiles            ProjectProductionUnitFile[]          @relation("ProjectProductionUnitFileAdder")
  productionApprovalAssignments       ProductionApprovalStep[]             @relation("ProductionApprovalRecipient")
  productionApprovalsRequested        ProductionApprovalStep[]             @relation("ProductionApprovalRequester")
  productionApprovalDecisions         ProductionApprovalStep[]             @relation("ProductionApprovalDecider")
  productionHandoversRequested        ProjectProductionHandover[]          @relation("ProjectProductionHandoverRequester")
  productionHandoverAssignments       ProjectProductionHandover[]          @relation("ProjectProductionHandoverRecipient")
  productionHandoversCompleted        ProjectProductionHandover[]          @relation("ProjectProductionHandoverCompleter")
  signedOffProductionSupervisions     ProjectProductionSupervision[]       @relation("ProjectProductionSupervisionSigner")
  createdProductionSampleRounds       ProductionSampleRound[]              @relation("ProductionSampleRoundCreator")
  completedProductionSampleRounds     ProductionSampleRound[]              @relation("ProductionSampleRoundCompleter")
  decidedPhysicalSampleRounds         ProductionSampleRound[]              @relation("ProductionSampleRoundDecider")
  productionSampleRequestAssignments  ProductionSampleRound[]              @relation("ProductionSampleRoundRecipient")
  productionSampleRoundParticipations ProductionSampleRoundParticipant[]   @relation("ProductionSampleRoundParticipantUser")
  addedProductionSampleParticipants   ProductionSampleRoundParticipant[]   @relation("ProductionSampleRoundParticipantAdder")
  addedProductionSampleEvidence       ProductionSampleRoundEvidence[]      @relation("ProductionSampleRoundEvidenceAdder")
  productionSampleFeedbackRecipients  ProductionSampleFeedback[]           @relation("ProductionSampleFeedbackRecipient")
  productionSampleFeedbackSent        ProductionSampleFeedback[]           @relation("ProductionSampleFeedbackSender")
  projectClosures                     ProjectClosure[]                     @relation("ProjectClosureCloser")
  projectFormDrafts                   ProjectFormDraft[]                   @relation("ProjectFormDraftOwner")
  createdAt                           DateTime                             @default(now())
  updatedAt                           DateTime                             @updatedAt
}

model UserArchiveAccess {
  id          String             @id @default(cuid())
  userId      String             @unique
  user        User               @relation("UserArchiveAccessUser", fields: [userId], references: [id], onDelete: Cascade)
  level       ArchiveAccessLevel @default(FULL)
  grantedById String?
  grantedBy   User?              @relation("UserArchiveAccessGrantedBy", fields: [grantedById], references: [id], onDelete: SetNull)
  createdAt   DateTime           @default(now())
  updatedAt   DateTime           @updatedAt

  @@index([grantedById])
}

model UserArchiveAssetAccess {
  id                    String               @id @default(cuid())
  userId                String
  user                  User                 @relation("UserArchiveAssetAccessUser", fields: [userId], references: [id], onDelete: Cascade)
  archivedProjectFileId String?
  archivedProjectFile   ArchivedProjectFile? @relation("UserArchiveAssetAccessArchivedProjectFile", fields: [archivedProjectFileId], references: [id], onDelete: Cascade)
  manualArchiveFileId   String?
  manualArchiveFile     ManualArchiveFile?   @relation("UserArchiveAssetAccessManualArchiveFile", fields: [manualArchiveFileId], references: [id], onDelete: Cascade)
  grantedById           String?
  grantedBy             User?                @relation("UserArchiveAssetAccessGrantedBy", fields: [grantedById], references: [id], onDelete: SetNull)
  createdAt             DateTime             @default(now())
  updatedAt             DateTime             @updatedAt

  @@unique([userId, archivedProjectFileId])
  @@unique([userId, manualArchiveFileId])
  @@index([userId])
  @@index([archivedProjectFileId])
  @@index([manualArchiveFileId])
  @@index([grantedById])
}

model PasswordResetToken {
  id        String    @id @default(cuid())
  userId    String
  user      User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  tokenHash String    @unique
  expiresAt DateTime
  usedAt    DateTime?
  createdAt DateTime  @default(now())

  @@index([userId])
  @@index([expiresAt])
}

model PermissionDefinition {
  id                          String                       @id @default(cuid())
  key                         String                       @unique
  label                       String
  description                 String
  group                       String
  isSystem                    Boolean                      @default(true)
  rolePermissions             RolePermission[]
  collaboratorTypePermissions CollaboratorTypePermission[]
  createdAt                   DateTime                     @default(now())
  updatedAt                   DateTime                     @updatedAt
}

model RolePermission {
  id            String               @id @default(cuid())
  role          UserRole
  permissionKey String
  permission    PermissionDefinition @relation(fields: [permissionKey], references: [key], onDelete: Restrict)
  enabled       Boolean              @default(true)
  createdAt     DateTime             @default(now())
  updatedAt     DateTime             @updatedAt

  @@unique([role, permissionKey])
  @@index([role])
  @@index([permissionKey])
}

model CollaboratorTypePermission {
  id               String               @id @default(cuid())
  collaboratorType CollaboratorType
  permissionKey    String
  permission       PermissionDefinition @relation(fields: [permissionKey], references: [key], onDelete: Restrict)
  enabled          Boolean              @default(true)
  createdAt        DateTime             @default(now())
  updatedAt        DateTime             @updatedAt

  @@unique([collaboratorType, permissionKey])
  @@index([collaboratorType])
  @@index([permissionKey])
}

model Project {
  id                     String                         @id @default(cuid())
  name                   String
  category               String?
  description            String?
  executionType          ProjectExecutionType?
  budgetRequired         Boolean?
  budget                 Int?
  currency               String?
  statusId               String?
  status                 ProjectStatusOption?           @relation(fields: [statusId], references: [id], onDelete: SetNull)
  priority               ProjectPriority?
  startDate              DateTime?
  endDate                DateTime?
  currentStageName       String?
  stageCount             Int?
  ownerId                String?
  owner                  User?                          @relation("ProjectOwner", fields: [ownerId], references: [id], onDelete: Restrict)
  createdById            String
  createdBy              User                           @relation("ProjectCreator", fields: [createdById], references: [id], onDelete: Restrict)
  isPinned               Boolean                        @default(false)
  stages                 ProjectStage[]
  tags                   ProjectTagAssignment[]
  revisions              ProjectRevision[]
  comments               ProjectComment[]
  comparisonComments     ComparisonComment[]
  attachments            ProjectAttachment[]
  invoiceRequests        StageInvoiceRequest[]
  activityLogs           ProjectActivityLog[]
  collaborators          ProjectCollaborator[]
  coOwners               ProjectCoOwner[]
  executors              ProjectExecutor[]
  stageFileHandoffs      ProjectStageFileHandoff[]
  fileChecklists         ProjectFileChecklist[]
  fileChecklistRequests  ProjectFileChecklistRequest[]
  productionUnits        ProjectProductionUnit[]
  productionSupervisions ProjectProductionSupervision[]
  productionSampleRounds ProductionSampleRound[]
  closure                ProjectClosure?
  archive                ProjectArchive?
  archivedFiles          ArchivedProjectFile[]
  archiveArtworkMetadata ArchiveArtworkMetadata[]
  completionWorkflow     ProjectCompletionWorkflow?
  completionDocuments    ProjectCompletionDocument[]
  workflowStages         ProjectWorkflowStage[]
  inquiry                ProjectInquiry?
  researchWorkspaces     ProjectResearchWorkspace[]
  conceptFolders         ProjectConceptFolder[]
  formDrafts             ProjectFormDraft[]
  completedAt            DateTime?
  archivedAt             DateTime?
  createdAt              DateTime                       @default(now())
  updatedAt              DateTime                       @updatedAt

  @@index([statusId])
  @@index([ownerId])
  @@index([isPinned])
  @@index([createdAt])
  @@index([createdById])
}

model ProjectFormDraft {
  id             String   @id @default(cuid())
  projectId      String
  project        Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)
  userId         String
  user           User     @relation("ProjectFormDraftOwner", fields: [userId], references: [id], onDelete: Cascade)
  formKey        String   @db.VarChar(191)
  payload        Json
  clientId       String   @db.VarChar(64)
  clientRevision Int      @default(0)
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@unique([projectId, userId, formKey])
  @@index([userId, updatedAt])
  @@index([projectId, updatedAt])
}

model ProjectCoOwner {
  projectId String
  project   Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)
  userId    String
  user      User     @relation("ProjectCoOwnerUser", fields: [userId], references: [id], onDelete: Cascade)
  addedById String?
  addedBy   User?    @relation("ProjectCoOwnerAdder", fields: [addedById], references: [id], onDelete: SetNull)
  createdAt DateTime @default(now())

  @@id([projectId, userId])
  @@index([userId])
  @@index([addedById])
}

model ProjectWorkflowStage {
  id          String                     @id @default(cuid())
  projectId   String
  project     Project                    @relation(fields: [projectId], references: [id], onDelete: Cascade)
  stageKey    ProjectWorkflowStageKey
  status      ProjectWorkflowStageStatus @default(LOCKED)
  unlockedAt  DateTime?
  completedAt DateTime?
  createdAt   DateTime                   @default(now())
  updatedAt   DateTime                   @updatedAt

  @@unique([projectId, stageKey])
  @@index([projectId, status])
  @@index([stageKey, status])
}

model ContactDirectoryEntry {
  id             String                @id @default(cuid())
  name           String
  company        String?
  position       String?
  email          String?
  phone          String?
  createdById    String
  createdBy      User                  @relation("ContactDirectoryEntryCreator", fields: [createdById], references: [id], onDelete: Restrict)
  inquiryParties ProjectInquiryParty[]
  createdAt      DateTime              @default(now())
  updatedAt      DateTime              @updatedAt

  @@index([name])
  @@index([email])
  @@index([createdById])
}

model ProjectInquiry {
  id                 String                       @id @default(cuid())
  projectId          String                       @unique
  project            Project                      @relation(fields: [projectId], references: [id], onDelete: Cascade)
  clientOrigin       ProjectInquiryClientOrigin?
  initialBrief       String?                      @db.Text
  businessObjectives String?                      @db.Text
  inquiryDate        DateTime?                    @db.Date
  deadline           DateTime?                    @db.Date
  legalNotes         String?                      @db.Text
  priority           ProjectInquiryPriority?
  parties            ProjectInquiryParty[]
  targetMarkets      ProjectInquiryTargetMarket[]
  deliverables       ProjectInquiryDeliverable[]
  attachments        ProjectInquiryAttachment[]
  createdAt          DateTime                     @default(now())
  updatedAt          DateTime                     @updatedAt

  @@index([createdAt])
}

model ProjectInquiryParty {
  id               String                    @id @default(cuid())
  inquiryId        String
  inquiry          ProjectInquiry            @relation(fields: [inquiryId], references: [id], onDelete: Cascade)
  role             ProjectInquiryPartyRole
  source           ProjectInquiryPartySource
  userId           String?
  user             User?                     @relation("ProjectInquiryPartyUser", fields: [userId], references: [id], onDelete: Restrict)
  contactId        String?
  contact          ContactDirectoryEntry?    @relation(fields: [contactId], references: [id], onDelete: Restrict)
  snapshotName     String
  snapshotCompany  String?
  snapshotPosition String?
  snapshotEmail    String?
  snapshotPhone    String?
  sequence         Int                       @default(0)
  createdAt        DateTime                  @default(now())
  updatedAt        DateTime                  @updatedAt

  @@index([inquiryId, role, sequence])
  @@index([userId])
  @@index([contactId])
}

model ProjectInquiryTargetMarket {
  id              String                         @id @default(cuid())
  inquiryId       String
  inquiry         ProjectInquiry                 @relation(fields: [inquiryId], references: [id], onDelete: Cascade)
  label           String
  normalizedLabel String
  kind            ProjectInquiryTargetMarketKind
  createdAt       DateTime                       @default(now())

  @@unique([inquiryId, normalizedLabel])
  @@index([normalizedLabel])
}

model ProjectInquiryDeliverable {
  id              String         @id @default(cuid())
  inquiryId       String
  inquiry         ProjectInquiry @relation(fields: [inquiryId], references: [id], onDelete: Cascade)
  label           String
  normalizedLabel String
  createdAt       DateTime       @default(now())

  @@unique([inquiryId, normalizedLabel])
  @@index([normalizedLabel])
}

model ProjectInquiryAttachment {
  inquiryId    String
  inquiry      ProjectInquiry                @relation(fields: [inquiryId], references: [id], onDelete: Cascade)
  attachmentId String
  attachment   ProjectAttachment             @relation(fields: [attachmentId], references: [id], onDelete: Cascade)
  field        ProjectInquiryAttachmentField
  createdAt    DateTime                      @default(now())

  @@id([inquiryId, attachmentId])
  @@unique([attachmentId])
  @@index([inquiryId, field])
}

model ProjectResearchWorkspace {
  id          String                  @id @default(cuid())
  projectId   String
  project     Project                 @relation(fields: [projectId], references: [id], onDelete: Cascade)
  ownerUserId String
  owner       User                    @relation("ProjectResearchWorkspaceOwner", fields: [ownerUserId], references: [id], onDelete: Cascade)
  folders     ProjectResearchFolder[]
  createdAt   DateTime                @default(now())
  updatedAt   DateTime                @updatedAt

  @@unique([projectId, ownerUserId])
  @@index([ownerUserId])
}

model ProjectResearchFolder {
  id             String                          @id @default(cuid())
  workspaceId    String
  workspace      ProjectResearchWorkspace        @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  name           String
  normalizedName String
  systemKey      ProjectResearchFolderSystemKey?
  isSystem       Boolean                         @default(false)
  sortOrder      Int                             @default(1000)
  createdById    String?
  createdBy      User?                           @relation("ProjectResearchFolderCreator", fields: [createdById], references: [id], onDelete: SetNull)
  files          ProjectResearchFolderFile[]
  createdAt      DateTime                        @default(now())
  updatedAt      DateTime                        @updatedAt

  @@unique([workspaceId, normalizedName])
  @@unique([workspaceId, systemKey])
  @@index([workspaceId, sortOrder])
  @@index([createdById])
}

model ProjectResearchFolderFile {
  id           String                @id @default(cuid())
  folderId     String
  folder       ProjectResearchFolder @relation(fields: [folderId], references: [id], onDelete: Cascade)
  attachmentId String                @unique
  attachment   ProjectAttachment     @relation(fields: [attachmentId], references: [id], onDelete: Cascade)
  addedById    String
  addedBy      User                  @relation("ProjectResearchFolderFileAdder", fields: [addedById], references: [id], onDelete: Restrict)
  createdAt    DateTime              @default(now())

  @@unique([folderId, attachmentId])
  @@index([folderId, createdAt])
  @@index([addedById])
}

model ProjectConceptFolder {
  id                               String                  @id @default(cuid())
  projectId                        String
  project                          Project                 @relation(fields: [projectId], references: [id], onDelete: Cascade)
  workflowStageKey                 ProjectWorkflowStageKey @default(CONCEPT_CREATION)
  taskerStageId                    String                  @unique
  taskerStage                      ProjectStage            @relation(fields: [taskerStageId], references: [id], onDelete: Cascade)
  assignedExecutorId               String?
  assignedExecutor                 ProjectExecutor?        @relation("ProjectConceptFolderAssignedExecutor", fields: [projectId, assignedExecutorId], references: [projectId, userId], onDelete: Restrict, onUpdate: Cascade)
  approvedAttachmentId             String?                 @unique
  approvedAttachment               ProjectAttachment?      @relation("ProjectConceptApprovedAttachment", fields: [approvedAttachmentId], references: [id], onDelete: Restrict)
  approvedById                     String?
  approvedBy                       User?                   @relation("ProjectConceptFolderApprover", fields: [approvedById], references: [id], onDelete: SetNull)
  approvedAt                       DateTime?
  sourceStage3ConceptId            String?                 @unique
  sourceStage3Concept              ProjectConceptFolder?   @relation("ProjectConceptPromotion", fields: [sourceStage3ConceptId], references: [id], onDelete: Restrict)
  promotedStage4Concept            ProjectConceptFolder?   @relation("ProjectConceptPromotion")
  sourceStage3ApprovedAttachmentId String?                 @unique
  sourceStage3ApprovedAttachment   ProjectAttachment?      @relation("ProjectConceptStartingReference", fields: [sourceStage3ApprovedAttachmentId], references: [id], onDelete: Restrict)
  name                             String
  normalizedName                   String
  sortOrder                        Int                     @default(1)
  createdById                      String?
  createdBy                        User?                   @relation("ProjectConceptFolderCreator", fields: [createdById], references: [id], onDelete: SetNull)
  createdAt                        DateTime                @default(now())
  updatedAt                        DateTime                @updatedAt

  @@unique([projectId, workflowStageKey, normalizedName])
  @@index([projectId, workflowStageKey, sortOrder])
  @@index([projectId, workflowStageKey, assignedExecutorId, sortOrder])
  @@index([approvedById])
  @@index([createdById])
}

model ProjectStageFileHandoff {
  id                     String                  @id @default(cuid())
  projectId              String
  project                Project                 @relation(fields: [projectId], references: [id], onDelete: Cascade)
  sourceWorkflowStageKey ProjectWorkflowStageKey
  sourceAttachmentId     String
  sourceAttachment       ProjectAttachment       @relation("ProjectStageFileHandoffSourceAttachment", fields: [sourceAttachmentId], references: [id], onDelete: Cascade)
  targetWorkflowStageKey ProjectWorkflowStageKey
  handedOffById          String
  handedOffBy            User                    @relation("ProjectStageFileHandoffCreator", fields: [handedOffById], references: [id], onDelete: Restrict)
  handedOffAt            DateTime                @default(now())
  checklist              ProjectFileChecklist?
  productionUnit         ProjectProductionUnit?
  createdAt              DateTime                @default(now())
  updatedAt              DateTime                @updatedAt

  @@unique([projectId, sourceAttachmentId, targetWorkflowStageKey])
  @@index([projectId, targetWorkflowStageKey, handedOffAt])
  @@index([sourceAttachmentId])
  @@index([handedOffById])
}

model ProjectFileChecklist {
  id                 String                        @id @default(cuid())
  projectId          String
  project            Project                       @relation(fields: [projectId], references: [id], onDelete: Cascade)
  handoffId          String                        @unique
  handoff            ProjectStageFileHandoff       @relation(fields: [handoffId], references: [id], onDelete: Cascade)
  sourceAttachmentId String
  sourceAttachment   ProjectAttachment             @relation("ProjectFileChecklistSourceAttachment", fields: [sourceAttachmentId], references: [id], onDelete: Cascade)
  items              ProjectFileChecklistItem[]
  requests           ProjectFileChecklistRequest[]
  productionUnit     ProjectProductionUnit?
  createdAt          DateTime                      @default(now())
  updatedAt          DateTime                      @updatedAt

  @@unique([projectId, sourceAttachmentId])
  @@index([projectId, updatedAt])
  @@index([sourceAttachmentId])
}

model ProjectFileChecklistItem {
  id          String                               @id @default(cuid())
  checklistId String
  checklist   ProjectFileChecklist                 @relation(fields: [checklistId], references: [id], onDelete: Cascade)
  fieldKey    ProjectFileChecklistField
  value       Json?
  status      ProjectFileChecklistItemStatus       @default(PENDING)
  updatedById String?
  updatedBy   User?                                @relation("ProjectFileChecklistItemUpdater", fields: [updatedById], references: [id], onDelete: SetNull)
  attachments ProjectFileChecklistItemAttachment[]
  requests    ProjectFileChecklistRequest[]
  createdAt   DateTime                             @default(now())
  updatedAt   DateTime                             @updatedAt

  @@unique([checklistId, fieldKey])
  @@index([checklistId, status])
  @@index([updatedById])
}

model ProjectFileChecklistItemAttachment {
  checklistItemId String
  checklistItem   ProjectFileChecklistItem @relation(fields: [checklistItemId], references: [id], onDelete: Cascade)
  attachmentId    String
  attachment      ProjectAttachment        @relation("ProjectFileChecklistItemAttachment", fields: [attachmentId], references: [id], onDelete: Cascade)
  createdAt       DateTime                 @default(now())

  @@id([checklistItemId, attachmentId])
  @@index([attachmentId])
}

model ProjectFileChecklistRequest {
  id                     String                                    @id @default(cuid())
  clientRequestId        String                                    @unique
  projectId              String
  project                Project                                   @relation(fields: [projectId], references: [id], onDelete: Cascade)
  checklistId            String
  checklist              ProjectFileChecklist                      @relation(fields: [checklistId], references: [id], onDelete: Cascade)
  checklistItemId        String
  checklistItem          ProjectFileChecklistItem                  @relation(fields: [checklistItemId], references: [id], onDelete: Cascade)
  fieldKey               ProjectFileChecklistField
  requestedById          String
  requestedBy            User                                      @relation("ProjectFileChecklistRequestRequester", fields: [requestedById], references: [id], onDelete: Restrict)
  channel                ProjectFileChecklistRequestChannel
  recipientUserId        String?
  recipientUser          User?                                     @relation("ProjectFileChecklistRequestRecipient", fields: [recipientUserId], references: [id], onDelete: Restrict)
  recipientName          String?
  recipientEmail         String?
  message                String?                                   @db.Text
  status                 ProjectFileChecklistRequestStatus         @default(PENDING)
  workflowStatus         ProjectFileChecklistRequestWorkflowStatus @default(REQUESTED)
  requestedAt            DateTime                                  @default(now())
  sentAt                 DateTime?
  failedAt               DateTime?
  failureMessage         String?                                   @db.Text
  acceptedAt             DateTime?
  completedAt            DateTime?
  declinedAt             DateTime?
  declineReason          String?                                   @db.Text
  respondedByUserId      String?
  respondedByUser        User?                                     @relation("ProjectFileChecklistRequestResponder", fields: [respondedByUserId], references: [id], onDelete: SetNull)
  responseSource         ProjectFileChecklistResponseSource?
  externalTokenHash      String?                                   @unique
  externalTokenExpiresAt DateTime?
  externalTokenCreatedAt DateTime?
  externalTokenRevokedAt DateTime?
  externalOpenedAt       DateTime?
  externalResponderName  String?
  externalResponderEmail String?
  responseAttachments    ProjectAttachment[]                       @relation("ProjectFileChecklistRequestResponseAttachments")
  createdAt              DateTime                                  @default(now())
  updatedAt              DateTime                                  @updatedAt

  @@index([projectId, requestedAt])
  @@index([checklistId, fieldKey, requestedAt])
  @@index([checklistItemId, requestedAt])
  @@index([requestedById])
  @@index([recipientUserId])
  @@index([status, requestedAt])
  @@index([workflowStatus, requestedAt])
  @@index([respondedByUserId])
  @@index([externalTokenExpiresAt])
  @@index([responseSource])
}

model ProjectProductionUnit {
  id                 String                        @id @default(cuid())
  projectId          String
  project            Project                       @relation(fields: [projectId], references: [id], onDelete: Cascade)
  sourceHandoffId    String                        @unique
  sourceHandoff      ProjectStageFileHandoff       @relation(fields: [sourceHandoffId], references: [id], onDelete: Cascade)
  sourceChecklistId  String                        @unique
  sourceChecklist    ProjectFileChecklist          @relation(fields: [sourceChecklistId], references: [id], onDelete: Cascade)
  sourceAttachmentId String
  sourceAttachment   ProjectAttachment             @relation("ProjectProductionUnitSourceAttachment", fields: [sourceAttachmentId], references: [id], onDelete: Cascade)
  status             ProjectProductionUnitStatus   @default(PREPARATION)
  createdById        String
  createdBy          User                          @relation("ProjectProductionUnitCreator", fields: [createdById], references: [id], onDelete: Restrict)
  files              ProjectProductionUnitFile[]
  approvalSteps      ProductionApprovalStep[]
  handover           ProjectProductionHandover?
  supervision        ProjectProductionSupervision?
  approvedAt         DateTime?
  handedOverAt       DateTime?
  createdAt          DateTime                      @default(now())
  updatedAt          DateTime                      @updatedAt

  @@unique([projectId, sourceAttachmentId])
  @@index([projectId, status, createdAt])
  @@index([sourceChecklistId])
  @@index([createdById])
}

model ProjectProductionSupervision {
  id               String                      @id @default(cuid())
  projectId        String
  project          Project                     @relation(fields: [projectId], references: [id], onDelete: Cascade)
  productionUnitId String                      @unique
  productionUnit   ProjectProductionUnit       @relation(fields: [productionUnitId], references: [id], onDelete: Cascade)
  status           ProductionSupervisionStatus @default(NOT_STARTED)
  signedOffById    String?
  signedOffBy      User?                       @relation("ProjectProductionSupervisionSigner", fields: [signedOffById], references: [id], onDelete: SetNull)
  signedOffAt      DateTime?
  sampleRounds     ProductionSampleRound[]
  createdAt        DateTime                    @default(now())
  updatedAt        DateTime                    @updatedAt

  @@unique([projectId, productionUnitId])
  @@index([projectId, status, createdAt])
  @@index([signedOffById])
}

model ProductionSampleRound {
  id                      String                             @id @default(cuid())
  clientRequestId         String?                            @unique
  projectId               String
  project                 Project                            @relation(fields: [projectId], references: [id], onDelete: Cascade)
  supervisionId           String
  supervision             ProjectProductionSupervision       @relation(fields: [supervisionId], references: [id], onDelete: Cascade)
  sequence                Int
  name                    String
  type                    ProductionSampleRoundType
  customTypeName          String?
  deadline                DateTime                           @db.Date
  recipientRoute          ProductionHandoverRoute?
  recipientType           ProductionApprovalRecipientType?
  recipientUserId         String?
  recipientUser           User?                              @relation("ProductionSampleRoundRecipient", fields: [recipientUserId], references: [id], onDelete: Restrict)
  recipientName           String?
  recipientEmail          String?
  recipientCompany        String?
  recipientPhone          String?
  requestNote             String?                            @db.Text
  requestReferenceFileIds String[]                           @default([])
  emailStatus             ProductionDispatchStatus           @default(NOT_SENT)
  emailSentAt             DateTime?
  emailError              String?                            @db.Text
  emailProviderMessageId  String?
  emailAttemptCount       Int                                @default(0)
  decision                PhysicalSampleDecision?
  decisionNote            String?                            @db.Text
  decidedById             String?
  decidedBy               User?                              @relation("ProductionSampleRoundDecider", fields: [decidedById], references: [id], onDelete: SetNull)
  decidedAt               DateTime?
  submissionDueAt         DateTime?
  submittedAt             DateTime?
  reviewDueAt             DateTime?
  reviewedAt              DateTime?
  revisionSignoffDueAt    DateTime?
  revisionSignedOffAt     DateTime?
  deliveryDueAt           DateTime?
  deliveredAt             DateTime?
  status                  ProductionSampleRoundStatus        @default(PENDING)
  overallDecision         ProductionSampleDecision?
  overallNotes            String?                            @db.Text
  completedById           String?
  completedBy             User?                              @relation("ProductionSampleRoundCompleter", fields: [completedById], references: [id], onDelete: SetNull)
  completedAt             DateTime?
  createdById             String
  createdBy               User                               @relation("ProductionSampleRoundCreator", fields: [createdById], references: [id], onDelete: Restrict)
  evaluations             ProductionSampleEvaluation[]
  participants            ProductionSampleRoundParticipant[]
  evidence                ProductionSampleRoundEvidence[]
  feedback                ProductionSampleFeedback[]
  createdAt               DateTime                           @default(now())
  updatedAt               DateTime                           @updatedAt

  @@unique([supervisionId, sequence])
  @@index([projectId, status, createdAt])
  @@index([projectId, deadline, status])
  @@index([projectId, decision, deadline])
  @@index([emailStatus, updatedAt])
  @@index([supervisionId, status, sequence])
  @@index([decidedById])
  @@index([recipientUserId])
  @@index([completedById])
  @@index([createdById])
}

model ProductionSampleEvaluation {
  id            String                    @id @default(cuid())
  sampleRoundId String
  sampleRound   ProductionSampleRound     @relation(fields: [sampleRoundId], references: [id], onDelete: Cascade)
  criterion     ProductionSampleCriterion
  decision      ProductionSampleDecision?
  comment       String?                   @db.Text
  createdAt     DateTime                  @default(now())
  updatedAt     DateTime                  @updatedAt

  @@unique([sampleRoundId, criterion])
  @@index([sampleRoundId])
}

model ProductionSampleRoundParticipant {
  sampleRoundId String
  sampleRound   ProductionSampleRound @relation(fields: [sampleRoundId], references: [id], onDelete: Cascade)
  userId        String
  user          User                  @relation("ProductionSampleRoundParticipantUser", fields: [userId], references: [id], onDelete: Cascade)
  addedById     String
  addedBy       User                  @relation("ProductionSampleRoundParticipantAdder", fields: [addedById], references: [id], onDelete: Restrict)
  addedAt       DateTime              @default(now())

  @@id([sampleRoundId, userId])
  @@index([userId])
  @@index([addedById])
}

model ProductionSampleRoundEvidence {
  id            String                     @id @default(cuid())
  sampleRoundId String
  sampleRound   ProductionSampleRound      @relation(fields: [sampleRoundId], references: [id], onDelete: Cascade)
  attachmentId  String                     @unique
  attachment    ProjectAttachment          @relation("ProductionSampleRoundEvidenceAttachment", fields: [attachmentId], references: [id], onDelete: Cascade)
  criterion     ProductionSampleCriterion?
  addedById     String
  addedBy       User                       @relation("ProductionSampleRoundEvidenceAdder", fields: [addedById], references: [id], onDelete: Restrict)
  createdAt     DateTime                   @default(now())

  @@unique([sampleRoundId, attachmentId])
  @@index([sampleRoundId, criterion, createdAt])
  @@index([addedById])
}

model ProductionSampleFeedback {
  id                String                          @id @default(cuid())
  clientRequestId   String                          @unique
  sampleRoundId     String
  sampleRound       ProductionSampleRound           @relation(fields: [sampleRoundId], references: [id], onDelete: Cascade)
  recipientType     ProductionApprovalRecipientType
  recipientUserId   String?
  recipientUser     User?                           @relation("ProductionSampleFeedbackRecipient", fields: [recipientUserId], references: [id], onDelete: SetNull)
  recipientName     String
  recipientEmail    String
  subject           String
  messageSnapshot   String                          @db.Text
  status            ProductionDispatchStatus        @default(PENDING)
  providerMessageId String?
  sentById          String
  sentBy            User                            @relation("ProductionSampleFeedbackSender", fields: [sentById], references: [id], onDelete: Restrict)
  sentAt            DateTime?
  failedAt          DateTime?
  error             String?                         @db.Text
  createdAt         DateTime                        @default(now())
  updatedAt         DateTime                        @updatedAt

  @@index([sampleRoundId, createdAt])
  @@index([recipientUserId])
  @@index([sentById])
  @@index([status, createdAt])
}

model ProjectClosure {
  id         String   @id @default(cuid())
  projectId  String   @unique
  project    Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)
  closedById String
  closedBy   User     @relation("ProjectClosureCloser", fields: [closedById], references: [id], onDelete: Restrict)
  closedAt   DateTime @default(now())
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  @@index([closedById])
  @@index([closedAt])
}

model ProjectProductionUnitFile {
  id               String                @id @default(cuid())
  productionUnitId String
  productionUnit   ProjectProductionUnit @relation(fields: [productionUnitId], references: [id], onDelete: Cascade)
  attachmentId     String                @unique
  attachment       ProjectAttachment     @relation("ProjectProductionUnitFileAttachment", fields: [attachmentId], references: [id], onDelete: Cascade)
  addedById        String
  addedBy          User                  @relation("ProjectProductionUnitFileAdder", fields: [addedById], references: [id], onDelete: Restrict)
  createdAt        DateTime              @default(now())

  @@unique([productionUnitId, attachmentId])
  @@index([productionUnitId, createdAt])
  @@index([addedById])
}

model ProductionApprovalStep {
  id                          String                           @id @default(cuid())
  clientRequestId             String?                          @unique
  productionUnitId            String
  productionUnit              ProjectProductionUnit            @relation(fields: [productionUnitId], references: [id], onDelete: Cascade)
  sequence                    Int
  isMarketingDirectorRequired Boolean                          @default(false)
  recipientType               ProductionApprovalRecipientType?
  recipientUserId             String?
  recipientUser               User?                            @relation("ProductionApprovalRecipient", fields: [recipientUserId], references: [id], onDelete: Restrict)
  recipientName               String?
  recipientEmail              String?
  requestedById               String?
  requestedBy                 User?                            @relation("ProductionApprovalRequester", fields: [requestedById], references: [id], onDelete: Restrict)
  sharedFieldKeys             ProjectFileChecklistField[]      @default([])
  selectedFileIds             String[]                         @default([])
  sharedSnapshot              Json?
  message                     String?                          @db.Text
  status                      ProductionApprovalStepStatus     @default(WAITING)
  dispatchStatus              ProductionDispatchStatus         @default(NOT_SENT)
  activatedAt                 DateTime?
  sentAt                      DateTime?
  failedAt                    DateTime?
  failureMessage              String?                          @db.Text
  externalTokenHash           String?                          @unique
  externalTokenExpiresAt      DateTime?
  externalTokenCreatedAt      DateTime?
  externalTokenRevokedAt      DateTime?
  externalOpenedAt            DateTime?
  decidedByUserId             String?
  decidedByUser               User?                            @relation("ProductionApprovalDecider", fields: [decidedByUserId], references: [id], onDelete: SetNull)
  decisionComment             String?                          @db.Text
  decidedAt                   DateTime?
  createdAt                   DateTime                         @default(now())
  updatedAt                   DateTime                         @updatedAt

  @@unique([productionUnitId, sequence])
  @@index([productionUnitId, status, sequence])
  @@index([recipientUserId, status])
  @@index([requestedById])
  @@index([dispatchStatus, status])
  @@index([decidedByUserId])
}

model ProjectProductionHandover {
  id                     String                           @id @default(cuid())
  clientRequestId        String                           @unique
  productionUnitId       String                           @unique
  productionUnit         ProjectProductionUnit            @relation(fields: [productionUnitId], references: [id], onDelete: Cascade)
  route                  ProductionHandoverRoute
  recipientType          ProductionApprovalRecipientType
  recipientUserId        String?
  recipientUser          User?                            @relation("ProjectProductionHandoverRecipient", fields: [recipientUserId], references: [id], onDelete: Restrict)
  recipientName          String
  recipientEmail         String
  recipientCompany       String?
  recipientPhone         String?
  sharedFieldKeys        ProjectFileChecklistField[]      @default([])
  selectedFileIds        String[]                         @default([])
  contentSnapshot        Json
  note                   String?                          @db.Text
  requestedById          String
  requestedBy            User                             @relation("ProjectProductionHandoverRequester", fields: [requestedById], references: [id], onDelete: Restrict)
  handedOverById         String?
  handedOverBy           User?                            @relation("ProjectProductionHandoverCompleter", fields: [handedOverById], references: [id], onDelete: Restrict)
  deliveryStatus         ProductionHandoverDeliveryStatus @default(PENDING)
  providerMessageId      String?
  sentAt                 DateTime?
  failedAt               DateTime?
  failureMessage         String?                          @db.Text
  externalTokenHash      String?                          @unique
  externalTokenExpiresAt DateTime?
  externalTokenCreatedAt DateTime?
  externalTokenRevokedAt DateTime?
  createdAt              DateTime                         @default(now())
  updatedAt              DateTime                         @updatedAt

  @@index([recipientUserId])
  @@index([deliveryStatus, createdAt])
  @@index([requestedById])
  @@index([handedOverById])
}

model ProjectCategory {
  id          String   @id @default(cuid())
  name        String   @unique
  description String?
  color       String?
  isActive    Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ProjectStatusOption {
  id          String                    @id @default(cuid())
  name        String
  slug        String                    @unique
  description String?
  color       String?
  groupId     String?
  group       ProjectStatusGroupOption? @relation(fields: [groupId], references: [id], onDelete: SetNull)
  sortOrder   Int                       @default(0)
  isActive    Boolean                   @default(true)
  isSystem    Boolean                   @default(false)
  projects    Project[]
  createdAt   DateTime                  @default(now())
  updatedAt   DateTime                  @updatedAt

  @@index([slug])
  @@index([groupId, isActive, sortOrder])
}

model ProjectStatusGroupOption {
  id          String                @id @default(cuid())
  name        String
  slug        String                @unique
  description String?
  color       String?
  sortOrder   Int                   @default(0)
  isActive    Boolean               @default(true)
  isSystem    Boolean               @default(false)
  statuses    ProjectStatusOption[]
  createdAt   DateTime              @default(now())
  updatedAt   DateTime              @updatedAt

  @@index([slug])
  @@index([isActive, sortOrder])
}

model ProjectTag {
  id          String                 @id @default(cuid())
  name        String                 @unique
  description String?
  color       String?
  isActive    Boolean                @default(true)
  projects    ProjectTagAssignment[]
  createdAt   DateTime               @default(now())
  updatedAt   DateTime               @updatedAt
}

model ProjectTagAssignment {
  projectId String
  tagId     String
  project   Project    @relation(fields: [projectId], references: [id], onDelete: Cascade)
  tag       ProjectTag @relation(fields: [tagId], references: [id], onDelete: Restrict)
  createdAt DateTime   @default(now())

  @@id([projectId, tagId])
  @@index([tagId])
}

model AssetTag {
  id                  String                                @id @default(cuid())
  name                String                                @unique
  description         String?
  color               String?
  isActive            Boolean                               @default(true)
  projectAttachments  ProjectAttachmentAssetTagAssignment[]
  manualArchiveFiles  ManualArchiveFileAssetTagAssignment[]
  manualLibraryAssets ManualLibraryAssetTagAssignment[]
  createdAt           DateTime                              @default(now())
  updatedAt           DateTime                              @updatedAt
}

model ArchiveCategory {
  id                 String                  @id @default(cuid())
  name               String
  slug               String                  @unique
  description        String?
  iconUrl            String?
  iconKey            String?
  color              String?
  parentId           String?
  parent             ArchiveCategory?        @relation("ArchiveCategoryHierarchy", fields: [parentId], references: [id], onDelete: SetNull)
  children           ArchiveCategory[]       @relation("ArchiveCategoryHierarchy")
  sortOrder          Int                     @default(0)
  isActive           Boolean                 @default(true)
  isSystem           Boolean                 @default(false)
  allowedUsers       ArchiveCategoryAccess[]
  projectArchives    ProjectArchive[]
  manualArchiveFiles ManualArchiveFile[]
  createdAt          DateTime                @default(now())
  updatedAt          DateTime                @updatedAt

  @@index([slug])
  @@index([parentId])
  @@index([isActive, sortOrder])
}

model ArchiveCategoryAccess {
  archiveCategoryId String
  userId            String
  createdAt         DateTime        @default(now())
  createdById       String?
  archiveCategory   ArchiveCategory @relation(fields: [archiveCategoryId], references: [id], onDelete: Cascade)
  user              User            @relation("ArchiveCategoryAccessUser", fields: [userId], references: [id], onDelete: Cascade)
  createdBy         User?           @relation("ArchiveCategoryAccessCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  @@id([archiveCategoryId, userId])
  @@index([userId])
  @@index([archiveCategoryId])
  @@index([createdById])
}

model ProjectAttachmentAssetTagAssignment {
  attachmentId String
  tagId        String
  attachment   ProjectAttachment @relation(fields: [attachmentId], references: [id], onDelete: Cascade)
  tag          AssetTag          @relation(fields: [tagId], references: [id], onDelete: Restrict)
  createdAt    DateTime          @default(now())

  @@id([attachmentId, tagId])
  @@index([tagId])
}

model ManualArchiveFileAssetTagAssignment {
  archiveFileId String
  tagId         String
  archiveFile   ManualArchiveFile @relation(fields: [archiveFileId], references: [id], onDelete: Cascade)
  tag           AssetTag          @relation(fields: [tagId], references: [id], onDelete: Restrict)
  createdAt     DateTime          @default(now())

  @@id([archiveFileId, tagId])
  @@index([tagId])
}

model ManualLibraryAssetTagAssignment {
  assetId   String
  tagId     String
  asset     ManualLibraryAsset @relation(fields: [assetId], references: [id], onDelete: Cascade)
  tag       AssetTag           @relation(fields: [tagId], references: [id], onDelete: Restrict)
  createdAt DateTime           @default(now())

  @@id([assetId, tagId])
  @@index([tagId])
}

model ProjectCollaborator {
  projectId                String
  project                  Project                              @relation(fields: [projectId], references: [id], onDelete: Cascade)
  userId                   String
  user                     User                                 @relation("ProjectCollaboratorUser", fields: [userId], references: [id], onDelete: Cascade)
  participantType          ProjectCollaboratorParticipantType?
  canInteract              Boolean                              @default(false)
  canAddCaptions           Boolean                              @default(false)
  canDownloadFiles         Boolean                              @default(false)
  canViewBudget            Boolean                              @default(false)
  canViewVendorInfo        Boolean                              @default(false)
  canAccessProjectArchives Boolean                              @default(false)
  chatVisibilityPaused     Boolean                              @default(false)
  addedById                String?
  addedBy                  User?                                @relation("ProjectCollaboratorAdder", fields: [addedById], references: [id], onDelete: SetNull)
  visibilityPauses         ProjectCollaboratorVisibilityPause[]
  createdAt                DateTime                             @default(now())

  @@id([projectId, userId])
  @@index([userId])
  @@index([addedById])
}

model ProjectExecutor {
  projectId              String
  project                Project                @relation(fields: [projectId], references: [id], onDelete: Cascade)
  userId                 String
  user                   User                   @relation("ProjectExecutorUser", fields: [userId], references: [id], onDelete: Cascade)
  assignedConceptFolders ProjectConceptFolder[] @relation("ProjectConceptFolderAssignedExecutor")
  addedById              String?
  addedBy                User?                  @relation("ProjectExecutorAddedBy", fields: [addedById], references: [id], onDelete: SetNull)
  createdAt              DateTime               @default(now())
  updatedAt              DateTime               @updatedAt

  @@id([projectId, userId])
  @@index([userId])
  @@index([addedById])
}

model ProjectCollaboratorVisibilityPause {
  id           String              @id @default(cuid())
  projectId    String
  userId       String
  collaborator ProjectCollaborator @relation(fields: [projectId, userId], references: [projectId, userId], onDelete: Cascade)
  pausedAt     DateTime
  resumedAt    DateTime?
  createdById  String
  createdBy    User                @relation("ProjectCollaboratorVisibilityPauseCreator", fields: [createdById], references: [id], onDelete: Restrict)
  createdAt    DateTime            @default(now())
  updatedAt    DateTime            @updatedAt

  @@index([projectId, userId, pausedAt])
  @@index([projectId, pausedAt, resumedAt])
  @@index([createdById])
}

model CalendarCollaborator {
  id        String   @id @default(cuid())
  userId    String   @unique
  user      User     @relation("CalendarCollaboratorUser", fields: [userId], references: [id], onDelete: Cascade)
  addedById String?
  addedBy   User?    @relation("CalendarCollaboratorAdder", fields: [addedById], references: [id], onDelete: SetNull)
  createdAt DateTime @default(now())

  @@index([addedById])
}

model ProjectStage {
  id                 String                @id @default(cuid())
  projectId          String
  project            Project               @relation(fields: [projectId], references: [id], onDelete: Cascade)
  name               String
  description        String?
  budget             Int?
  plannedStartAt     DateTime?
  plannedDueAt       DateTime?
  invoiceRequired    Boolean               @default(true)
  isTasker           Boolean               @default(false)
  actualStartedAt    DateTime?
  startedById        String?
  startedBy          User?                 @relation("ProjectStageStarter", fields: [startedById], references: [id], onDelete: SetNull)
  status             StageStatus           @default(PENDING)
  order              Int
  revisions          ProjectRevision[]
  comments           ProjectComment[]
  comparisonComments ComparisonComment[]
  attachments        ProjectAttachment[]
  invoiceRequests    StageInvoiceRequest[]
  archives           ProjectArchive[]      @relation("ProjectArchiveFinalStage")
  activityLogs       ProjectActivityLog[]
  conceptFolder      ProjectConceptFolder?
  completedAt        DateTime?
  createdAt          DateTime              @default(now())
  updatedAt          DateTime              @updatedAt

  @@index([projectId, order])
  @@index([projectId, isTasker, order])
  @@index([startedById])
}

model ProjectRevision {
  id              String                @id @default(cuid())
  projectId       String
  project         Project               @relation(fields: [projectId], references: [id], onDelete: Cascade)
  stageId         String
  stage           ProjectStage          @relation(fields: [stageId], references: [id], onDelete: Cascade)
  createdById     String
  createdBy       User                  @relation(fields: [createdById], references: [id], onDelete: Restrict)
  reviewedById    String?
  reviewedBy      User?                 @relation("ProjectRevisionReviewer", fields: [reviewedById], references: [id], onDelete: SetNull)
  revisionNumber  Int
  title           String
  summary         String?
  status          ProjectRevisionStatus @default(PENDING_REVIEW)
  reviewedAt      DateTime?
  rejectionReason String?
  attachments     ProjectAttachment[]
  archivedFiles   ArchivedProjectFile[] @relation("ArchivedProjectFileSourceRevision")
  comments        ProjectComment[]
  activityLogs    ProjectActivityLog[]
  createdAt       DateTime              @default(now())
  updatedAt       DateTime              @updatedAt

  @@unique([stageId, revisionNumber])
  @@index([projectId, stageId, createdAt])
  @@index([createdById])
  @@index([reviewedById])
  @@index([status])
}

model ProjectComment {
  id              String                  @id @default(cuid())
  projectId       String
  project         Project                 @relation(fields: [projectId], references: [id], onDelete: Cascade)
  stageId         String
  stage           ProjectStage            @relation(fields: [stageId], references: [id], onDelete: Cascade)
  revisionId      String?
  revision        ProjectRevision?        @relation(fields: [revisionId], references: [id], onDelete: SetNull)
  authorId        String
  author          User                    @relation(fields: [authorId], references: [id], onDelete: Restrict)
  body            String
  deletedAt       DateTime?
  deletedByUserId String?
  deletedByUser   User?                   @relation("ProjectCommentDeletedBy", fields: [deletedByUserId], references: [id], onDelete: SetNull)
  attachments     ProjectAttachment[]
  mentions        ProjectCommentMention[]
  createdAt       DateTime                @default(now())
  updatedAt       DateTime                @updatedAt

  @@index([projectId, stageId, createdAt])
  @@index([revisionId])
  @@index([authorId])
  @@index([deletedByUserId])
}

model ProjectCommentMention {
  id              String         @id @default(cuid())
  commentId       String
  comment         ProjectComment @relation(fields: [commentId], references: [id], onDelete: Cascade)
  mentionedUserId String
  mentionedUser   User           @relation(fields: [mentionedUserId], references: [id], onDelete: Cascade)
  createdAt       DateTime       @default(now())

  @@unique([commentId, mentionedUserId])
  @@index([mentionedUserId, createdAt])
  @@index([commentId, createdAt])
}

model FluxAiConversation {
  id            String                   @id @default(cuid())
  userId        String
  user          User                     @relation(fields: [userId], references: [id], onDelete: Cascade)
  title         String
  status        FluxAiConversationStatus @default(ACTIVE)
  lastMessageAt DateTime                 @default(now())
  archivedAt    DateTime?
  messages      FluxAiMessage[]
  createdAt     DateTime                 @default(now())
  updatedAt     DateTime                 @updatedAt

  @@index([userId, status, lastMessageAt])
  @@index([userId, createdAt])
}

model FluxAiMessage {
  id                String             @id @default(cuid())
  conversationId    String
  conversation      FluxAiConversation @relation(fields: [conversationId], references: [id], onDelete: Cascade)
  role              FluxAiMessageRole
  content           String
  responseType      String?
  structuredPayload Json?
  createdAt         DateTime           @default(now())

  @@index([conversationId, createdAt])
  @@index([conversationId, role, createdAt])
}

model FluxAiSearchHistory {
  id              String   @id @default(cuid())
  userId          String
  user            User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  query           String   @db.VarChar(240)
  normalizedQuery String   @db.VarChar(240)
  searchedAt      DateTime @default(now())

  @@unique([userId, normalizedQuery])
  @@index([userId, searchedAt])
}

model ProjectAttachment {
  id                         String                                @id @default(cuid())
  projectId                  String
  project                    Project                               @relation(fields: [projectId], references: [id], onDelete: Cascade)
  stageId                    String?
  stage                      ProjectStage?                         @relation(fields: [stageId], references: [id], onDelete: Cascade)
  revisionId                 String?
  revision                   ProjectRevision?                      @relation(fields: [revisionId], references: [id], onDelete: SetNull)
  commentId                  String?
  comment                    ProjectComment?                       @relation(fields: [commentId], references: [id], onDelete: SetNull)
  uploadedById               String
  uploadedBy                 User                                  @relation(fields: [uploadedById], references: [id], onDelete: Restrict)
  reviewedById               String?
  reviewedBy                 User?                                 @relation("SubmissionReviewer", fields: [reviewedById], references: [id], onDelete: SetNull)
  baseComparisonComments     ComparisonComment[]                   @relation("ComparisonCommentBaseAttachment")
  compareComparisonComments  ComparisonComment[]                   @relation("ComparisonCommentCompareAttachment")
  captionComparisonComments  ComparisonComment[]                   @relation("ComparisonCommentCaptionAttachment")
  fileFavorites              FileFavorite[]
  fileName                   String
  originalFileName           String
  mimeType                   String
  fileSize                   Int
  bucket                     String
  storageKey                 String                                @unique
  assetType                  AttachmentAssetType
  status                     AttachmentStatus                      @default(UPLOADING)
  submissionReviewStatus     SubmissionReviewStatus?
  reviewedAt                 DateTime?
  reviewNote                 String?
  archivedFiles              ArchivedProjectFile[]                 @relation("ArchivedProjectFileSourceAttachment")
  archiveArtworkMetadata     ArchiveArtworkMetadata[]              @relation("ArchiveArtworkMetadataSourceAttachment")
  assetTags                  ProjectAttachmentAssetTagAssignment[]
  inquiryAssociations        ProjectInquiryAttachment[]
  researchFolderFile         ProjectResearchFolderFile?
  stageFileHandoffs          ProjectStageFileHandoff[]             @relation("ProjectStageFileHandoffSourceAttachment")
  sourceFileChecklists       ProjectFileChecklist[]                @relation("ProjectFileChecklistSourceAttachment")
  sourceProductionUnits      ProjectProductionUnit[]               @relation("ProjectProductionUnitSourceAttachment")
  productionUnitFile         ProjectProductionUnitFile?            @relation("ProjectProductionUnitFileAttachment")
  sampleRoundEvidence        ProductionSampleRoundEvidence?        @relation("ProductionSampleRoundEvidenceAttachment")
  approvedConceptFolder      ProjectConceptFolder?                 @relation("ProjectConceptApprovedAttachment")
  conceptStartingReference   ProjectConceptFolder?                 @relation("ProjectConceptStartingReference")
  fileChecklistItems         ProjectFileChecklistItemAttachment[]  @relation("ProjectFileChecklistItemAttachment")
  checklistResponseRequestId String?
  checklistResponseRequest   ProjectFileChecklistRequest?          @relation("ProjectFileChecklistRequestResponseAttachments", fields: [checklistResponseRequestId], references: [id], onDelete: SetNull)
  uploadSource               ProjectAttachmentUploadSource         @default(AUTHENTICATED_USER)
  externalUploaderName       String?
  externalUploaderEmail      String?
  createdAt                  DateTime                              @default(now())
  updatedAt                  DateTime                              @updatedAt

  @@index([projectId, createdAt])
  @@index([stageId, createdAt])
  @@index([revisionId, createdAt])
  @@index([commentId, createdAt])
  @@index([uploadedById])
  @@index([reviewedById])
  @@index([checklistResponseRequestId])
  @@index([uploadSource])
  @@index([status])
  @@index([submissionReviewStatus])
}

model StageInvoiceRequest {
  id              String       @id @default(cuid())
  projectId       String
  project         Project      @relation(fields: [projectId], references: [id], onDelete: Cascade)
  stageId         String
  stage           ProjectStage @relation(fields: [stageId], references: [id], onDelete: Cascade)
  requestedById   String
  requestedBy     User         @relation("StageInvoiceRequestRequester", fields: [requestedById], references: [id], onDelete: Restrict)
  requestedFromId String
  requestedFrom   User         @relation("StageInvoiceRequestRecipient", fields: [requestedFromId], references: [id], onDelete: Restrict)
  note            String?
  fulfilledAt     DateTime?
  createdAt       DateTime     @default(now())
  updatedAt       DateTime     @updatedAt

  @@unique([stageId])
  @@index([projectId, createdAt])
  @@index([requestedById])
  @@index([requestedFromId])
  @@index([fulfilledAt])
}

model FileFavorite {
  id           String            @id @default(cuid())
  userId       String
  user         User              @relation(fields: [userId], references: [id], onDelete: Cascade)
  attachmentId String
  attachment   ProjectAttachment @relation(fields: [attachmentId], references: [id], onDelete: Cascade)
  createdAt    DateTime          @default(now())

  @@unique([userId, attachmentId])
  @@index([userId])
  @@index([attachmentId])
  @@index([userId, createdAt])
}

model ManualLibraryAssetFavorite {
  id                   String             @id @default(cuid())
  userId               String
  user                 User               @relation(fields: [userId], references: [id], onDelete: Cascade)
  manualLibraryAssetId String
  manualLibraryAsset   ManualLibraryAsset @relation(fields: [manualLibraryAssetId], references: [id], onDelete: Cascade)
  createdAt            DateTime           @default(now())

  @@unique([userId, manualLibraryAssetId])
  @@index([userId])
  @@index([manualLibraryAssetId])
  @@index([userId, createdAt])
}

model ComparisonComment {
  id                  String             @id @default(cuid())
  projectId           String
  project             Project            @relation(fields: [projectId], references: [id], onDelete: Cascade)
  stageId             String
  stage               ProjectStage       @relation(fields: [stageId], references: [id], onDelete: Cascade)
  baseAttachmentId    String
  baseAttachment      ProjectAttachment  @relation("ComparisonCommentBaseAttachment", fields: [baseAttachmentId], references: [id], onDelete: Cascade)
  compareAttachmentId String
  compareAttachment   ProjectAttachment  @relation("ComparisonCommentCompareAttachment", fields: [compareAttachmentId], references: [id], onDelete: Cascade)
  captionAttachmentId String?
  captionAttachment   ProjectAttachment? @relation("ComparisonCommentCaptionAttachment", fields: [captionAttachmentId], references: [id], onDelete: SetNull)
  isCaption           Boolean            @default(false)
  comparisonOpacity   Float?
  xPercent            Float
  yPercent            Float
  body                String
  createdById         String
  createdBy           User               @relation(fields: [createdById], references: [id], onDelete: Restrict)
  createdAt           DateTime           @default(now())
  updatedAt           DateTime           @updatedAt

  @@index([projectId, stageId, createdAt])
  @@index([baseAttachmentId, compareAttachmentId, createdAt])
  @@index([captionAttachmentId, createdAt])
  @@index([projectId, stageId, isCaption, createdAt])
  @@index([createdById])
}

model ProjectActivityLog {
  id         String            @id @default(cuid())
  projectId  String
  project    Project           @relation(fields: [projectId], references: [id], onDelete: Cascade)
  stageId    String?
  stage      ProjectStage?     @relation(fields: [stageId], references: [id], onDelete: Cascade)
  revisionId String?
  revision   ProjectRevision?  @relation(fields: [revisionId], references: [id], onDelete: SetNull)
  actorId    String
  actor      User              @relation(fields: [actorId], references: [id], onDelete: Restrict)
  action     ActivityLogAction
  metadata   Json?
  createdAt  DateTime          @default(now())

  @@index([projectId, createdAt])
  @@index([stageId, createdAt])
  @@index([revisionId, createdAt])
  @@index([actorId])
}

model ProjectArchive {
  id                String                @id @default(cuid())
  projectId         String                @unique
  project           Project               @relation(fields: [projectId], references: [id], onDelete: Cascade)
  finalStageId      String?
  finalStage        ProjectStage?         @relation("ProjectArchiveFinalStage", fields: [finalStageId], references: [id], onDelete: Restrict)
  archivedById      String
  archivedBy        User                  @relation("ProjectArchiveArchivedBy", fields: [archivedById], references: [id], onDelete: Restrict)
  projectName       String
  projectCategory   String
  projectTag        String?
  archiveCategoryId String?
  archiveCategory   ArchiveCategory?      @relation(fields: [archiveCategoryId], references: [id], onDelete: SetNull)
  status            ArchiveRecordStatus   @default(ARCHIVED)
  archivedAt        DateTime
  files             ArchivedProjectFile[]
  createdAt         DateTime              @default(now())
  updatedAt         DateTime              @updatedAt

  @@index([archiveCategoryId, archivedAt])
  @@index([archivedById])
}

model ArchivedProjectFile {
  id                   String                   @id @default(cuid())
  archiveId            String
  archive              ProjectArchive           @relation(fields: [archiveId], references: [id], onDelete: Cascade)
  projectId            String
  project              Project                  @relation(fields: [projectId], references: [id], onDelete: Cascade)
  sourceAttachmentId   String
  sourceAttachment     ProjectAttachment        @relation("ArchivedProjectFileSourceAttachment", fields: [sourceAttachmentId], references: [id], onDelete: Restrict)
  sourceRevisionId     String?
  sourceRevision       ProjectRevision?         @relation("ArchivedProjectFileSourceRevision", fields: [sourceRevisionId], references: [id], onDelete: SetNull)
  finalArchiveFileName String
  originalFileName     String
  mimeType             String
  fileSize             Int
  bucket               String
  storageKey           String
  archivedById         String
  archivedBy           User                     @relation("ArchivedProjectFileArchivedBy", fields: [archivedById], references: [id], onDelete: Restrict)
  artworkMetadata      ArchiveArtworkMetadata?
  userArchiveAccesses  UserArchiveAssetAccess[] @relation("UserArchiveAssetAccessArchivedProjectFile")
  archivedAt           DateTime
  createdAt            DateTime                 @default(now())
  updatedAt            DateTime                 @updatedAt

  @@unique([archiveId, finalArchiveFileName])
  @@unique([archiveId, sourceAttachmentId])
  @@index([projectId, archivedAt])
  @@index([sourceAttachmentId])
  @@index([sourceRevisionId])
  @@index([archivedById])
}

model ArchiveArtworkMetadata {
  id                  String                           @id @default(cuid())
  sourceType          ArchiveArtworkMetadataSourceType @default(PROJECT_FINAL_FILE)
  archiveFileId       String?                          @unique
  archiveFile         ArchivedProjectFile?             @relation(fields: [archiveFileId], references: [id], onDelete: Cascade)
  manualArchiveFileId String?                          @unique
  manualArchiveFile   ManualArchiveFile?               @relation(fields: [manualArchiveFileId], references: [id], onDelete: Cascade)
  projectId           String?
  project             Project?                         @relation(fields: [projectId], references: [id], onDelete: Cascade)
  sourceAttachmentId  String?
  sourceAttachment    ProjectAttachment?               @relation("ArchiveArtworkMetadataSourceAttachment", fields: [sourceAttachmentId], references: [id], onDelete: Restrict)
  artworkId           String
  titleWorkingName    String
  versionRevision     String
  languageMarket      String
  artworkType         String
  brandSubBrand       String
  productSku          String?
  campaignProject     String?
  formatDimensions    String?
  colourSpace         String
  resolution          String?
  fileFormats         String
  printProcess        String?
  specialFinishes     String?
  creationDate        DateTime
  lastModifiedDate    DateTime
  goLiveOnShelfDate   DateTime?
  expirySunsetDate    DateTime?
  archiveStatus       String
  createdByName       String
  createdByUserId     String?
  createdByUser       User?                            @relation("ArchiveArtworkMetadataCreatedBy", fields: [createdByUserId], references: [id], onDelete: SetNull)
  approvedByName      String
  approvedByUserId    String?
  approvedByUser      User?                            @relation("ArchiveArtworkMetadataApprovedBy", fields: [approvedByUserId], references: [id], onDelete: SetNull)
  approvedAt          DateTime?
  clientBrandOwner    String
  regulatoryClearance String?
  fontsUsed           String
  imagesPhotography   String
  illustrationsIcons  String
  colourCodes         String
  thirdPartyLogosIp   String?
  supplierPrinter     String?
  outputFilesList     String?
  printProofRef       String?
  packagingDielineRef String?
  changeLog           String
  relatedArtworks     String?
  briefSpecLink       String?
  generalNotes        String?
  archivedById        String
  archivedBy          User                             @relation("ArchiveArtworkMetadataArchivedBy", fields: [archivedById], references: [id], onDelete: Restrict)
  createdAt           DateTime                         @default(now())
  updatedAt           DateTime                         @updatedAt

  @@index([projectId])
  @@index([manualArchiveFileId])
  @@index([sourceAttachmentId])
  @@index([artworkId])
  @@index([titleWorkingName])
  @@index([brandSubBrand])
  @@index([artworkType])
  @@index([languageMarket])
  @@index([archiveStatus])
  @@index([archivedById])
}

enum ArchiveArtworkMetadataSourceType {
  PROJECT_FINAL_FILE
  DIRECT_UPLOAD
}

model ProjectCompletionWorkflow {
  id                              String                      @id @default(cuid())
  projectId                       String                      @unique
  project                         Project                     @relation(fields: [projectId], references: [id], onDelete: Cascade)
  approvalRequired                Boolean?
  approvalStatus                  ProjectCompletionStepStatus @default(NOT_STARTED)
  approvalContactUserId           String?
  approvalContactUser             User?                       @relation("ProjectCompletionApprovalContact", fields: [approvalContactUserId], references: [id], onDelete: SetNull)
  approvalNote                    String?
  approvalSelectedArchivedFileIds String[]                    @default([])
  approvalSelectedProjectFileIds  String[]                    @default([])
  approvalRequestedAt             DateTime?
  approvalCompletedAt             DateTime?
  copyrightRequired               Boolean?
  copyrightStatus                 ProjectCompletionStepStatus @default(NOT_STARTED)
  copyrightContactUserId          String?
  copyrightContactUser            User?                       @relation("ProjectCompletionCopyrightContact", fields: [copyrightContactUserId], references: [id], onDelete: SetNull)
  copyrightNote                   String?
  copyrightRequestedAt            DateTime?
  copyrightCompletedAt            DateTime?
  invoiceRequired                 Boolean?
  invoiceStatus                   ProjectCompletionStepStatus @default(NOT_STARTED)
  invoiceContactUserId            String?
  invoiceContactUser              User?                       @relation("ProjectCompletionInvoiceContact", fields: [invoiceContactUserId], references: [id], onDelete: SetNull)
  invoiceNote                     String?
  invoiceRequestedAt              DateTime?
  invoiceCompletedAt              DateTime?
  completedAt                     DateTime?
  documents                       ProjectCompletionDocument[]
  createdAt                       DateTime                    @default(now())
  updatedAt                       DateTime                    @updatedAt

  @@index([approvalStatus])
  @@index([copyrightStatus])
  @@index([invoiceStatus])
  @@index([approvalContactUserId])
  @@index([copyrightContactUserId])
  @@index([invoiceContactUserId])
}

model ProjectCompletionDocument {
  id               String                        @id @default(cuid())
  projectId        String
  project          Project                       @relation(fields: [projectId], references: [id], onDelete: Cascade)
  workflowId       String
  workflow         ProjectCompletionWorkflow     @relation(fields: [workflowId], references: [id], onDelete: Cascade)
  type             ProjectCompletionDocumentType
  originalFileName String
  archiveFileName  String
  mimeType         String
  fileSize         Int
  bucket           String
  storageKey       String                        @unique
  uploadedById     String
  uploadedBy       User                          @relation("ProjectCompletionDocumentUploadedBy", fields: [uploadedById], references: [id], onDelete: Restrict)
  uploadedAt       DateTime
  createdAt        DateTime                      @default(now())
  updatedAt        DateTime                      @updatedAt

  @@unique([workflowId, type])
  @@index([projectId, type, uploadedAt])
  @@index([uploadedById])
}

model ManualArchiveFile {
  id                  String                                @id @default(cuid())
  fileName            String
  originalFileName    String
  projectName         String?
  projectCreatedBy    String?
  archiveCategoryId   String?
  archiveCategory     ArchiveCategory?                      @relation(fields: [archiveCategoryId], references: [id], onDelete: SetNull)
  projectDate         DateTime?
  mimeType            String
  fileSize            Int
  bucket              String
  storageKey          String                                @unique
  status              AttachmentStatus                      @default(UPLOADING)
  uploadedById        String
  uploadedBy          User                                  @relation("ManualArchiveFileUploadedBy", fields: [uploadedById], references: [id], onDelete: Restrict)
  assetTags           ManualArchiveFileAssetTagAssignment[]
  artworkMetadata     ArchiveArtworkMetadata?
  userArchiveAccesses UserArchiveAssetAccess[]              @relation("UserArchiveAssetAccessManualArchiveFile")
  uploadedAt          DateTime                              @default(now())
  createdAt           DateTime                              @default(now())
  updatedAt           DateTime                              @updatedAt

  @@index([archiveCategoryId, uploadedAt])
  @@index([uploadedById])
  @@index([status])
}

model ManualLibraryAsset {
  id                          String                            @id @default(cuid())
  assetName                   String
  originalFileName            String
  createdByName               String?
  description                 String?
  category                    String?
  mimeType                    String
  fileSize                    Int
  bucket                      String
  storageKey                  String                            @unique
  status                      AttachmentStatus                  @default(UPLOADING)
  uploadedById                String
  uploadedBy                  User                              @relation("ManualLibraryAssetUploadedBy", fields: [uploadedById], references: [id], onDelete: Restrict)
  assetTags                   ManualLibraryAssetTagAssignment[]
  manualLibraryAssetFavorites ManualLibraryAssetFavorite[]
  uploadedAt                  DateTime                          @default(now())
  createdAt                   DateTime                          @default(now())
  updatedAt                   DateTime                          @updatedAt

  @@index([category, uploadedAt])
  @@index([uploadedById])
  @@index([status])
}

model Notification {
  id           String                  @id @default(cuid())
  userId       String
  user         User                    @relation(fields: [userId], references: [id], onDelete: Cascade)
  type         NotificationType
  title        String
  message      String
  entityType   NotificationEntityType?
  entityId     String?
  projectId    String?
  stageId      String?
  revisionId   String?
  commentId    String?
  attachmentId String?
  archiveId    String?
  url          String?
  dedupeKey    String?                 @unique
  isRead       Boolean                 @default(false)
  readAt       DateTime?
  createdAt    DateTime                @default(now())
  updatedAt    DateTime                @updatedAt

  @@index([userId, createdAt])
  @@index([userId, isRead, createdAt])
  @@index([projectId, createdAt])
  @@index([type, createdAt])
}

model Session {
  id        String   @id @default(cuid())
  token     String   @unique
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  expiresAt DateTime
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([userId])
  @@index([expiresAt])
}

model CalendarEvent {
  id          String            @id @default(cuid())
  title       String
  details     String?
  startAt     DateTime
  endAt       DateTime
  type        CalendarEventType
  tone        CalendarEventTone
  createdById String
  createdBy   User              @relation(fields: [createdById], references: [id], onDelete: Cascade)
  createdAt   DateTime          @default(now())
  updatedAt   DateTime          @updatedAt

  @@index([startAt])
  @@index([endAt])
  @@index([createdById])
}
